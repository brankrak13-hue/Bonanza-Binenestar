// This component is no longer needed after migrating from Firebase to Supabase.
// Kept as a stub to avoid breaking any imports that may still reference it.
export function FirebaseErrorListener() {
  return null;
}
